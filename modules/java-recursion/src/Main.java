public class Main {
    // Recursion: a method that calls itself, with a base case that stops
    // the calls once the problem is small enough to answer directly. A
    // few of these are problems you already solved with a loop earlier in
    // the curriculum - same answer, a different tool. Compare your two
    // solutions once both exist.

    // TODO: write a method named factorial that takes an int parameter n
    // and returns n! recursively. Base case: 0! and 1! are both 1.
    // Otherwise: n * factorial(n - 1). You wrote this with a loop in the
    // Methods lesson.

    // TODO: write a method named fibonacci that takes an int parameter n
    // and returns the nth Fibonacci number recursively, 0-indexed
    // (fibonacci(0) = 0, fibonacci(1) = 1). Base case: for n less than 2,
    // return n. Otherwise: fibonacci(n - 1) + fibonacci(n - 2).

    // TODO: write a method named sumDigits that takes an int parameter n
    // (always 0 or greater) and returns the sum of its digits
    // recursively. Base case: if n is less than 10, return n. Otherwise:
    // (n % 10) + sumDigits(n / 10).

    // TODO: write a method named reverseString that takes a String
    // parameter s and returns it reversed, recursively. Base case: a
    // String of length 0 or 1 is already its own reverse. Otherwise: the
    // last character, followed by reverseString of everything except the
    // last character. You wrote this with a loop in the Methods lesson -
    // no loop this time.

    public static void main(String[] args) {
        // TODO: try your methods out here - call them with a few
        // different inputs and click Run to see the output.
    }
}
